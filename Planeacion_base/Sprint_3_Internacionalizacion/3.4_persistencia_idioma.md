# Tarea 3.4: Persistencia de Idioma

## 🟡 PRIORIDAD MEDIA

## Objetivo
Guardar idioma seleccionado en user_preferences.

## Integración

### 1. Backend - ya existe campo `language` en user_preferences

### 2. Frontend - Sincronizar
```tsx
const changeLanguage = async (newLocale: string) => {
  // Cambiar UI
  router.push(pathname, { locale: newLocale });
  
  // Guardar en backend
  await fetch('/api/preferences', {
    method: 'PUT',
    body: JSON.stringify({ language: newLocale })
  });
};
```

### 3. Cargar al inicio
```tsx
useEffect(() => {
  const loadPreferences = async () => {
    const prefs = await fetch('/api/preferences').then(r => r.json());
    if (prefs.language) {
      router.push(pathname, { locale: prefs.language });
    }
  };
  loadPreferences();
}, []);
```

## Criterios de Éxito
- [ ] Idioma persiste entre sesiones
- [ ] Se carga automáticamente al iniciar
- [ ] Sincronización backend ↔ frontend funciona

⚠️ Actualizar PROGRESS.md al completar
