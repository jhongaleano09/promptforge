# Tarea 2.4: Validación en Tiempo Real

## 🟡 PRIORIDAD MEDIA

## Objetivo  
Implementar validación de configuración en tiempo real con feedback visual inmediato.

## Características

### 1. Validación de API Keys
- Formato correcto según provider
- Validación async al perder foco (blur)
- Indicador visual: ✓ válida, ✗ inválida, ⟳ validando

### 2. Validación de Modelos
- Verificar modelo disponible para provider
- Sugerir alternativas si no disponible

### 3. Feedback Visual
```tsx
<input 
  className={isValid ? 'border-green-500' : 'border-red-500'}
/>
{isValid && <span className="text-green-600">✓ Válida</span>}
{error && <span className="text-red-600">{error}</span>}
```

## Archivos
- `frontend/src/lib/validators.ts` - CREAR validadores
- `frontend/src/components/settings/` - Agregar validación inline
- `backend/app/api/providers.py` - Endpoint `/validate`

## Criterios de Éxito
- [ ] Validación de API key < 1 segundo
- [ ] Feedback visual claro
- [ ] No bloquea UI durante validación
- [ ] Mensajes de error útiles

⚠️ Actualizar PROGRESS.md al completar
