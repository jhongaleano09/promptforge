# Tarea 2.3: Sistema de Preferencias de Usuario

## 🟠 PRIORIDAD ALTA

## Objetivo
Utilizar y expandir la tabla `user_preferences` existente para persistir configuraciones del usuario entre sesiones.

## Archivos Principales
- `backend/app/db/models.py` - Modelo UserPreferences (ya existe)
- `backend/app/api/user_preferences.py` - Endpoints (expandir)
- `frontend/src/store/preferenceStore.ts` - CREAR store persistente

## Pasos Clave

### 1. Expandir Modelo UserPreferences
Agregar campos:
- `default_provider` - Provider preferido
- `default_model` - Modelo preferido  
- `auto_save_preferences` - Boolean
- `theme` - 'light' | 'dark' (para futuro)

### 2. Sincronización con Zustand
Crear middleware de persistencia que sincronice store con backend

### 3. Endpoints
- GET `/api/preferences` - Obtener preferencias
- PUT `/api/preferences` - Actualizar preferencias
- POST `/api/preferences/reset` - Resetear a defaults

## Criterios de Éxito
- [ ] Preferencias persisten entre sesiones
- [ ] Sincronización automática store ↔ backend
- [ ] UI carga preferencias al inicio
- [ ] Cambios se guardan automáticamente (o con botón según preferencia)

⚠️ Actualizar PROGRESS.md al completar
