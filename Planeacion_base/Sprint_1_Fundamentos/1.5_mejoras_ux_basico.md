# Tarea 1.5: Mejoras de UX Básico

## 🟢 PRIORIDAD MEDIA-BAJA

## Objetivo
Implementar mejoras básicas de experiencia de usuario que hacen la aplicación más profesional y usable. Estas son mejoras "quick wins" que tienen alto impacto visual con poco esfuerzo de desarrollo.

## Alcance de Mejoras

### 1. Tarjeta de Contexto (Context Card)
Mostrar información del contexto actual de generación:
- Tokens estimados
- Modelo seleccionado
- Proveedor (OpenAI, Anthropic, etc.)

### 2. Agregar Clave de Traducción "loading"
Corregir el warning de clave faltante en sistema i18n

### 3. Estados de Carga Mejorados
Indicadores visuales claros cuando el sistema está procesando

### 4. Mensajes de Error User-Friendly
Reemplazar errores técnicos con mensajes comprensibles

### 5. Feedback Visual de Acciones
Confirmaciones cuando el usuario realiza acciones (guardar settings, etc.)

## Estado Actual

### Problemas Identificados:
- ❌ Usuario no sabe qué modelo o proveedor está usando sin ir a Settings
- ❌ Missing translation key "loading" causa warning
- ❌ Estados de carga genéricos (solo "Loading..." sin contexto)
- ❌ Errores técnicos se muestran directamente al usuario
- ❌ No hay feedback cuando se guardan settings (usuario no sabe si funcionó)

### Archivos Relevantes:
```
Frontend:
- frontend/src/components/arena/ChatInterface.tsx    # Chat principal
- frontend/src/components/arena/ContextCard.tsx      # A CREAR
- frontend/src/components/ui/LoadingState.tsx        # A CREAR/MEJORAR
- frontend/src/components/ui/ErrorMessage.tsx        # A CREAR
- frontend/src/components/ui/Toast.tsx               # A CREAR (notifications)
- frontend/src/store/workflowStore.ts                # Estado del workflow
- frontend/src/store/settingsStore.ts                # Settings (si existe)
- frontend/public/i18n/spanish.json                  # Traducciones ES
- frontend/public/i18n/english.json                  # Traducciones EN

Backend:
- backend/app/api/workflow.py                        # Para token estimation
```

## Pasos de Implementación

### MEJORA 1: Tarjeta de Contexto (Context Card)

#### Objetivo:
Mostrar al usuario información relevante sobre el contexto actual de generación

#### Diseño Propuesto:
```
┌─────────────────────────────────────┐
│  Chat Principal                     │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ 🤖 OpenAI • gpt-4    📊 ~500 tokens│ │
│ └─────────────────────────────────┘ │
│                                     │
│  Usuario: Crea un logo...          │
│  ...                                │
└─────────────────────────────────────┘
```

#### Paso 1.1: Crear Componente ContextCard

**Crear**: `frontend/src/components/arena/ContextCard.tsx`

```typescript
'use client';

import { useSettingsStore } from '@/store/settingsStore'; // Ajustar path
import { useWorkflowStore } from '@/store/workflowStore'; // Ajustar path
import { useLanguage } from '@/store/languageStore'; // Ajustar path

interface ContextCardProps {
  estimatedTokens?: number;
  className?: string;
}

export default function ContextCard({ 
  estimatedTokens, 
  className = '' 
}: ContextCardProps) {
  const { t } = useLanguage();
  const { provider, model } = useSettingsStore();
  const { messages } = useWorkflowStore();

  // Calcular tokens estimados si no se proporciona
  const tokens = estimatedTokens || estimateTokens(messages);

  return (
    <div className={`
      flex items-center justify-between 
      px-4 py-2 
      bg-gray-50 border border-gray-200 rounded-lg
      text-sm text-gray-600
      ${className}
    `}>
      {/* Provider y Modelo */}
      <div className="flex items-center gap-2">
        <span className="text-base">{getProviderIcon(provider)}</span>
        <span className="font-medium">{provider}</span>
        <span className="text-gray-400">•</span>
        <span>{model || 'No model selected'}</span>
      </div>

      {/* Tokens estimados */}
      <div className="flex items-center gap-2">
        <span className="text-base">📊</span>
        <span>
          {t('context.estimatedTokens') || 'Est.'} 
          <span className="font-medium ml-1">~{tokens.toLocaleString()}</span>
        </span>
      </div>
    </div>
  );
}

// Helper: Estimar tokens (aproximación simple)
function estimateTokens(messages: any[]): number {
  if (!messages || messages.length === 0) return 0;
  
  const totalChars = messages.reduce((acc, msg) => {
    return acc + (msg.content?.length || 0);
  }, 0);
  
  // Aproximación: 1 token ≈ 4 caracteres en inglés/español
  return Math.ceil(totalChars / 4);
}

// Helper: Icono según provider
function getProviderIcon(provider: string): string {
  const icons: Record<string, string> = {
    'openai': '🤖',
    'anthropic': '🧠',
    'google': '🔍',
    'default': '⚡'
  };
  
  return icons[provider?.toLowerCase()] || icons.default;
}
```

#### Paso 1.2: Integrar ContextCard en ChatInterface

**Modificar**: `frontend/src/components/arena/ChatInterface.tsx`

```typescript
import ContextCard from './ContextCard';

export default function ChatInterface() {
  // ... código existente ...
  
  return (
    <div className="flex flex-col h-screen">
      {/* Agregar ContextCard arriba del chat */}
      <div className="p-4 border-b">
        <ContextCard />
      </div>
      
      {/* Mensajes del chat */}
      <div className="flex-1 overflow-y-auto">
        {/* ... mensajes existentes ... */}
      </div>
      
      {/* Input del usuario */}
      <div className="p-4 border-t">
        {/* ... input existente ... */}
      </div>
    </div>
  );
}
```

#### Paso 1.3: Agregar Traducciones para Context Card

**Modificar**: `frontend/public/i18n/spanish.json`
```json
{
  "context": {
    "estimatedTokens": "Tokens estimados",
    "noModel": "Sin modelo seleccionado",
    "configure": "Configurar"
  }
}
```

**Modificar**: `frontend/public/i18n/english.json`
```json
{
  "context": {
    "estimatedTokens": "Estimated tokens",
    "noModel": "No model selected",
    "configure": "Configure"
  }
}
```

---

### MEJORA 2: Agregar Clave de Traducción "loading"

#### Objetivo:
Corregir warning de missing translation key

#### Paso 2.1: Agregar Claves Faltantes

**Modificar**: `frontend/public/i18n/spanish.json`
```json
{
  "loading": "Cargando...",
  "loadingStates": {
    "default": "Cargando...",
    "generating": "Generando respuesta...",
    "analyzing": "Analizando solicitud...",
    "clarifying": "Preparando preguntas...",
    "finalizing": "Finalizando..."
  }
}
```

**Modificar**: `frontend/public/i18n/english.json`
```json
{
  "loading": "Loading...",
  "loadingStates": {
    "default": "Loading...",
    "generating": "Generating response...",
    "analyzing": "Analyzing request...",
    "clarifying": "Preparing questions...",
    "finalizing": "Finalizing..."
  }
}
```

#### Paso 2.2: Usar Claves en Componentes

**Buscar usos de "loading" hardcoded**:
```bash
grep -r "loading" frontend/src --include="*.tsx" --include="*.ts"
```

**Reemplazar** con traducciones:
```typescript
// ❌ Antes
<div>Loading...</div>

// ✅ Después
<div>{t('loading')}</div>
```

---

### MEJORA 3: Estados de Carga Mejorados

#### Objetivo:
Indicadores visuales claros y contextuales

#### Paso 3.1: Crear Componente LoadingState

**Crear**: `frontend/src/components/ui/LoadingState.tsx`

```typescript
'use client';

import { useLanguage } from '@/store/languageStore';

interface LoadingStateProps {
  type?: 'default' | 'generating' | 'analyzing' | 'clarifying' | 'finalizing';
  message?: string;
  size?: 'sm' | 'md' | 'lg';
}

export default function LoadingState({ 
  type = 'default',
  message,
  size = 'md' 
}: LoadingStateProps) {
  const { t } = useLanguage();
  
  const displayMessage = message || t(`loadingStates.${type}`) || t('loading');
  
  const sizeClasses = {
    sm: 'w-4 h-4',
    md: 'w-8 h-8',
    lg: 'w-12 h-12'
  };

  return (
    <div className="flex items-center justify-center gap-3 py-4">
      {/* Spinner animado */}
      <div className={`
        ${sizeClasses[size]}
        border-3 border-gray-200 border-t-blue-600
        rounded-full animate-spin
      `} />
      
      {/* Mensaje */}
      <span className="text-gray-600 animate-pulse">
        {displayMessage}
      </span>
    </div>
  );
}
```

#### Paso 3.2: Usar LoadingState en Chat

**Modificar**: `frontend/src/components/arena/ChatInterface.tsx`

```typescript
import LoadingState from '@/components/ui/LoadingState';

export default function ChatInterface() {
  const { isLoading, currentState } = useWorkflowStore();
  
  return (
    <div className="messages">
      {messages.map(msg => (
        <Message key={msg.id} {...msg} />
      ))}
      
      {/* Mostrar loading contextual */}
      {isLoading && (
        <LoadingState 
          type={currentState} // 'analyzing', 'generating', etc.
        />
      )}
    </div>
  );
}
```

---

### MEJORA 4: Mensajes de Error User-Friendly

#### Objetivo:
Convertir errores técnicos en mensajes comprensibles

#### Paso 4.1: Crear Componente ErrorMessage

**Crear**: `frontend/src/components/ui/ErrorMessage.tsx`

```typescript
'use client';

import { useLanguage } from '@/store/languageStore';

interface ErrorMessageProps {
  error: Error | string;
  retry?: () => void;
  onDismiss?: () => void;
}

export default function ErrorMessage({ 
  error, 
  retry, 
  onDismiss 
}: ErrorMessageProps) {
  const { t } = useLanguage();
  
  const errorMessage = typeof error === 'string' 
    ? error 
    : error.message;
  
  // Convertir errores técnicos a user-friendly
  const friendlyMessage = getFriendlyErrorMessage(errorMessage, t);

  return (
    <div className="
      p-4 mb-4
      bg-red-50 border border-red-200 rounded-lg
      flex items-start gap-3
    ">
      {/* Icono de error */}
      <span className="text-2xl">⚠️</span>
      
      <div className="flex-1">
        {/* Mensaje user-friendly */}
        <p className="text-red-800 font-medium mb-1">
          {friendlyMessage.title}
        </p>
        <p className="text-red-600 text-sm">
          {friendlyMessage.description}
        </p>
        
        {/* Acciones */}
        <div className="flex gap-2 mt-3">
          {retry && (
            <button
              onClick={retry}
              className="px-3 py-1 bg-red-600 text-white rounded hover:bg-red-700 text-sm"
            >
              {t('errors.retry') || 'Reintentar'}
            </button>
          )}
          {onDismiss && (
            <button
              onClick={onDismiss}
              className="px-3 py-1 bg-gray-200 text-gray-700 rounded hover:bg-gray-300 text-sm"
            >
              {t('errors.dismiss') || 'Cerrar'}
            </button>
          )}
        </div>
      </div>
    </div>
  );
}

// Helper: Convertir errores técnicos a user-friendly
function getFriendlyErrorMessage(
  technicalError: string, 
  t: (key: string) => string
): { title: string; description: string } {
  
  // Mapeo de errores comunes
  const errorMappings: Record<string, { title: string; description: string }> = {
    'network': {
      title: t('errors.networkTitle') || 'Error de conexión',
      description: t('errors.networkDesc') || 'No se pudo conectar al servidor. Verifica tu conexión a internet.'
    },
    'api_key': {
      title: t('errors.apiKeyTitle') || 'API key inválida',
      description: t('errors.apiKeyDesc') || 'La clave de API no es válida. Ve a Configuración para actualizarla.'
    },
    'timeout': {
      title: t('errors.timeoutTitle') || 'Tiempo de espera agotado',
      description: t('errors.timeoutDesc') || 'La operación tardó demasiado. Por favor intenta de nuevo.'
    },
    'rate_limit': {
      title: t('errors.rateLimitTitle') || 'Límite de uso excedido',
      description: t('errors.rateLimitDesc') || 'Has alcanzado el límite de solicitudes. Espera un momento antes de continuar.'
    }
  };

  // Detectar tipo de error
  const errorLower = technicalError.toLowerCase();
  
  if (errorLower.includes('network') || errorLower.includes('fetch failed')) {
    return errorMappings.network;
  }
  if (errorLower.includes('api key') || errorLower.includes('unauthorized')) {
    return errorMappings.api_key;
  }
  if (errorLower.includes('timeout')) {
    return errorMappings.timeout;
  }
  if (errorLower.includes('rate limit') || errorLower.includes('429')) {
    return errorMappings.rate_limit;
  }

  // Error genérico
  return {
    title: t('errors.genericTitle') || 'Algo salió mal',
    description: t('errors.genericDesc') || 'Ocurrió un error inesperado. Por favor intenta de nuevo.'
  };
}
```

#### Paso 4.2: Agregar Traducciones de Errores

**Modificar**: `frontend/public/i18n/spanish.json`
```json
{
  "errors": {
    "retry": "Reintentar",
    "dismiss": "Cerrar",
    "networkTitle": "Error de conexión",
    "networkDesc": "No se pudo conectar al servidor. Verifica tu conexión a internet.",
    "apiKeyTitle": "API key inválida",
    "apiKeyDesc": "La clave de API no es válida. Ve a Configuración para actualizarla.",
    "timeoutTitle": "Tiempo de espera agotado",
    "timeoutDesc": "La operación tardó demasiado. Por favor intenta de nuevo.",
    "rateLimitTitle": "Límite de uso excedido",
    "rateLimitDesc": "Has alcanzado el límite de solicitudes. Espera un momento.",
    "genericTitle": "Algo salió mal",
    "genericDesc": "Ocurrió un error inesperado. Por favor intenta de nuevo."
  }
}
```

(Repetir en `english.json` con traducciones correspondientes)

---

### MEJORA 5: Feedback Visual de Acciones (Toast Notifications)

#### Objetivo:
Confirmar acciones del usuario con notificaciones no invasivas

#### Paso 5.1: Crear Sistema de Toasts

**Crear**: `frontend/src/components/ui/Toast.tsx`

```typescript
'use client';

import { useEffect } from 'react';

export interface ToastProps {
  id: string;
  type: 'success' | 'error' | 'info' | 'warning';
  message: string;
  duration?: number;
  onDismiss: (id: string) => void;
}

export default function Toast({ 
  id, 
  type, 
  message, 
  duration = 3000,
  onDismiss 
}: ToastProps) {
  
  useEffect(() => {
    const timer = setTimeout(() => {
      onDismiss(id);
    }, duration);
    
    return () => clearTimeout(timer);
  }, [id, duration, onDismiss]);

  const styles = {
    success: 'bg-green-50 border-green-200 text-green-800',
    error: 'bg-red-50 border-red-200 text-red-800',
    info: 'bg-blue-50 border-blue-200 text-blue-800',
    warning: 'bg-yellow-50 border-yellow-200 text-yellow-800'
  };

  const icons = {
    success: '✅',
    error: '❌',
    info: 'ℹ️',
    warning: '⚠️'
  };

  return (
    <div className={`
      ${styles[type]}
      px-4 py-3 rounded-lg border
      flex items-center gap-3
      shadow-lg
      animate-slide-in-right
    `}>
      <span className="text-xl">{icons[type]}</span>
      <p className="flex-1 font-medium">{message}</p>
      <button
        onClick={() => onDismiss(id)}
        className="text-gray-500 hover:text-gray-700"
      >
        ✕
      </button>
    </div>
  );
}
```

**Crear**: `frontend/src/components/ui/ToastContainer.tsx`

```typescript
'use client';

import { create } from 'zustand';
import Toast, { ToastProps } from './Toast';

// Store de toasts
interface ToastStore {
  toasts: ToastProps[];
  addToast: (toast: Omit<ToastProps, 'id' | 'onDismiss'>) => void;
  removeToast: (id: string) => void;
}

export const useToastStore = create<ToastStore>((set) => ({
  toasts: [],
  addToast: (toast) => {
    const id = Date.now().toString();
    set((state) => ({
      toasts: [...state.toasts, { ...toast, id, onDismiss: () => {} }]
    }));
  },
  removeToast: (id) => {
    set((state) => ({
      toasts: state.toasts.filter((t) => t.id !== id)
    }));
  }
}));

// Componente contenedor
export default function ToastContainer() {
  const { toasts, removeToast } = useToastStore();

  return (
    <div className="fixed top-4 right-4 z-50 flex flex-col gap-2 max-w-sm">
      {toasts.map((toast) => (
        <Toast
          key={toast.id}
          {...toast}
          onDismiss={removeToast}
        />
      ))}
    </div>
  );
}
```

#### Paso 5.2: Integrar ToastContainer en Layout

**Modificar**: `frontend/src/app/layout.tsx`

```typescript
import ToastContainer from '@/components/ui/ToastContainer';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="es">
      <body>
        {children}
        <ToastContainer />  {/* ← AGREGAR */}
      </body>
    </html>
  );
}
```

#### Paso 5.3: Usar Toasts en Settings

**Modificar**: `frontend/src/app/settings/page.tsx`

```typescript
import { useToastStore } from '@/components/ui/ToastContainer';

export default function SettingsPage() {
  const { addToast } = useToastStore();
  const { t } = useLanguage();
  
  const handleSaveSettings = async () => {
    try {
      await saveSettings();
      
      // Mostrar toast de éxito
      addToast({
        type: 'success',
        message: t('settings.saved') || 'Configuración guardada correctamente'
      });
    } catch (error) {
      // Mostrar toast de error
      addToast({
        type: 'error',
        message: t('settings.saveFailed') || 'Error al guardar configuración'
      });
    }
  };
  
  return (
    // ... componente ...
  );
}
```

#### Paso 5.4: Agregar Animación CSS

**Crear/Modificar**: `frontend/src/app/globals.css`

```css
@keyframes slide-in-right {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.animate-slide-in-right {
  animation: slide-in-right 0.3s ease-out;
}
```

---

## Testing de Mejoras

### Test Manual Checklist:

#### Context Card:
- [ ] Se muestra correctamente arriba del chat
- [ ] Muestra provider correcto (OpenAI, Anthropic, etc.)
- [ ] Muestra modelo seleccionado
- [ ] Tokens estimados se actualizan al escribir
- [ ] Responsive en mobile/tablet/desktop

#### Translation "loading":
- [ ] No hay warnings de missing keys en console
- [ ] Traducciones funcionan en ES y EN
- [ ] Estados de carga contextuales se muestran correctamente

#### Loading States:
- [ ] Spinner animado se ve bien
- [ ] Mensajes contextuales apropiados según estado
- [ ] No bloquea UI innecesariamente

#### Error Messages:
- [ ] Errores técnicos se convierten a user-friendly
- [ ] Botón "Reintentar" funciona
- [ ] Botón "Cerrar" oculta el error
- [ ] Estilos consistentes con diseño

#### Toast Notifications:
- [ ] Toasts aparecen en esquina superior derecha
- [ ] Auto-dismiss después de 3 segundos
- [ ] Pueden cerrarse manualmente con ✕
- [ ] Múltiples toasts se apilan correctamente
- [ ] Animación de entrada suave

---

## Consideraciones Importantes

### Performance:
- Context Card re-renders: optimizar con `useMemo` si es necesario
- Toast animations: usar CSS en vez de JS cuando sea posible
- Token estimation: no hacer cálculos pesados en cada keystroke

### Accesibilidad:
- Toasts deben tener `role="alert"` para screen readers
- Loading states con `aria-live="polite"`
- Error messages con color + texto (no solo color)

### UX:
- No sobrecargar con demasiada información en Context Card
- Toasts no deben bloquear interacción
- Estados de carga: mostrar solo cuando sea >500ms

---

## Criterios de Éxito

### Funcionales:
- [ ] Context Card muestra información correcta
- [ ] No hay warnings de translation keys
- [ ] Estados de carga contextuales funcionan
- [ ] Errores se muestran user-friendly
- [ ] Toasts confirman acciones exitosas

### UX/UI:
- [ ] Interfaz se siente más "pulida" y profesional
- [ ] Usuario siempre sabe qué está pasando (loading, success, error)
- [ ] Feedback inmediato en todas las acciones
- [ ] Consistent visual design

### Técnicos:
- [ ] Componentes reutilizables y bien tipados
- [ ] No hay performance issues
- [ ] Código sigue convenciones del proyecto
- [ ] Tests manuales pasan completamente

---

## Archivos a Crear/Modificar

### Crear:
1. `frontend/src/components/arena/ContextCard.tsx`
2. `frontend/src/components/ui/LoadingState.tsx`
3. `frontend/src/components/ui/ErrorMessage.tsx`
4. `frontend/src/components/ui/Toast.tsx`
5. `frontend/src/components/ui/ToastContainer.tsx`

### Modificar:
1. `frontend/src/components/arena/ChatInterface.tsx` - Integrar ContextCard y LoadingState
2. `frontend/src/app/layout.tsx` - Integrar ToastContainer
3. `frontend/src/app/settings/page.tsx` - Usar toasts para feedback
4. `frontend/public/i18n/spanish.json` - Agregar todas las traducciones
5. `frontend/public/i18n/english.json` - Agregar todas las traducciones
6. `frontend/src/app/globals.css` - Agregar animaciones

---

## Resultado Esperado

### Antes:
- Usuario no sabe qué modelo usa sin ir a Settings
- "Loading..." genérico sin contexto
- Errores técnicos como "fetch failed" confusos
- Sin feedback al guardar settings (¿funcionó?)

### Después:
- Context Card siempre visible con info relevante
- Estados de carga contextuales ("Generando respuesta...")
- Errores user-friendly con opción de reintentar
- Toasts confirman acciones ("✅ Configuración guardada")

---

## Notas Importantes

⚠️ **PRIORIDAD**: Media-Baja - Mejora UX pero no es bloqueante

📝 **Modular**: Implementar mejoras de forma incremental, cada una es independiente

🔄 **PROGRESS.md**: Al completar esta tarea, actualizar el archivo `PROGRESS.md` marcando la tarea 1.5 como completada

🎨 **Diseño**: Mantener consistencia visual con resto de la aplicación

♿ **Accesibilidad**: No olvidar ARIA labels y soporte de teclado

---

## Referencias

- Toast notifications UX: https://www.nngroup.com/articles/toast-notifications/
- Error message best practices: https://www.nngroup.com/articles/error-message-guidelines/
- Loading states: https://www.nngroup.com/articles/progress-indicators/
- Token estimation: https://help.openai.com/en/articles/4936856-what-are-tokens-and-how-to-count-them
