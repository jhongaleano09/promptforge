# Tarea 4.1: Workflows Especializados

## 🔴 PRIORIDAD CRÍTICA

## Objetivo
Crear LangGraph workflows específicos para cada tipo de prompt.

## Tipos de Prompt

### 1. System Prompt
Workflow enfocado en generar prompts de sistema para chatbots/assistants

### 2. Image Prompt
Workflow optimizado para generar prompts de generación de imágenes (DALL-E, Midjourney, etc.)

### 3. Additional Prompt
Workflow para refinamiento y mejora de prompts existentes

## Estructura

**Crear archivos** (resolución de imports en workflow_factory.py):
- `backend/app/agents/system_prompt_graph.py`
- `backend/app/agents/image_prompt_graph.py`
- `backend/app/agents/additional_prompt_graph.py`

Cada uno debe tener estructura similar a `graph.py` pero con nodos especializados.

## Ejemplo: System Prompt Graph

```python
# system_prompt_graph.py
from langgraph.graph import StateGraph, END

def create_system_prompt_workflow():
    workflow = StateGraph(GraphState)
    
    workflow.add_node("analyze_requirements", analyze_system_requirements)
    workflow.add_node("define_personality", define_bot_personality)
    workflow.add_node("set_boundaries", set_behavior_boundaries)
    workflow.add_node("generate_system_prompt", generate_final_system_prompt)
    
    # Edges...
    
    return workflow.compile()
```

## Criterios de Éxito
- [ ] 3 workflows creados y funcionando
- [ ] Cada workflow tiene nodos especializados
- [ ] workflow_factory puede instanciar cada tipo
- [ ] Tests básicos pasan

⚠️ Actualizar PROGRESS.md al completar
