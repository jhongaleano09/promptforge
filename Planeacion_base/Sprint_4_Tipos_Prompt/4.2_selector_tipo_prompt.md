# Tarea 4.2: Selector de Tipo de Prompt

## 🟠 PRIORIDAD ALTA

## Objetivo
UI para seleccionar tipo de prompt antes de iniciar generación.

## Diseño

```
┌─────────────────────────────────────┐
│  ¿Qué tipo de prompt deseas crear?  │
│                                     │
│  [ ] 🤖 System Prompt              │
│      Para chatbots y assistants     │
│                                     │
│  [ ] 🎨 Image Prompt               │
│      Para generación de imágenes    │
│                                     │
│  [ ] ➕ Additional Prompt          │
│      Mejorar prompts existentes     │
│                                     │
│              [Continuar]            │
└─────────────────────────────────────┘
```

## Componente

**Crear**: `frontend/src/components/arena/PromptTypeSelector.tsx`

```tsx
interface PromptType {
  id: string;
  name: string;
  description: string;
  icon: string;
}

export default function PromptTypeSelector({ onSelect }) {
  const types: PromptType[] = [
    {
      id: 'system',
      name: 'System Prompt',
      description: 'Para chatbots y assistants',
      icon: '🤖'
    },
    // ...
  ];
  
  return (
    <div className="grid gap-4">
      {types.map(type => (
        <button onClick={() => onSelect(type.id)}>
          {type.icon} {type.name}
          <p>{type.description}</p>
        </button>
      ))}
    </div>
  );
}
```

## Criterios de Éxito
- [ ] Selector visible al iniciar conversación
- [ ] Cards bien diseñadas y descriptivas
- [ ] Selección se envía al backend
- [ ] Backend usa workflow correcto según tipo

⚠️ Actualizar PROGRESS.md al completar
